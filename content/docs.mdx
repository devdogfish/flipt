import {
  LLM_PROMPT,
  DECKS_LIST_RESPONSE,
  DECK_CREATE_BODY, DECK_CREATE_RESPONSE, DECK_CREATE_CURL, DECK_CREATE_FETCH,
  DECK_GET_RESPONSE,
  DECK_PATCH_BODY, DECK_PATCH_RESPONSE,
  CARDS_LIST_RESPONSE,
  CARD_CREATE_BODY, CARD_CREATE_RESPONSE,
  CARD_PATCH_BODY, CARD_PATCH_RESPONSE,
  COLLECTIONS_LIST_RESPONSE, COLLECTIONS_COURSES_RESPONSE,
  COLLECTION_GET_RESPONSE,
  COLLECTION_CREATE_BODY, COLLECTION_CREATE_RESPONSE,
  COURSE_COLLECTION_CREATE_BODY, COURSE_COLLECTION_CREATE_RESPONSE,
  COLLECTION_PATCH_BODY, COLLECTION_PATCH_RESPONSE,
  COLLECTION_ADD_DECK_RESPONSE,
  DECK_EXPORT_RESPONSE,
} from "@/content/docs-samples";

## What's what

Before anything else — here's how the pieces fit together.

**Cards** are the individual question-and-answer pairs. Every card has a front (the question or prompt) and a back (the answer).

**Decks** are collections of cards on a single topic — like "PSYC 1011 Midterm" or "Organic Chemistry: Reactions." You study a deck, share a deck, and manage cards inside a deck.

**Courses** are Dal course pages, like BIOL 1010 or STAT 1060. Each course page shows all the public decks Dal students have contributed for that course. You don't own a course — it's a shared community space. Anyone can add their deck to a course, and the community helps keep the cards accurate.

**Collections** are your own private folders. If you want to group your personal decks together (e.g. "Fall semester" or "Finals prep"), you can put them in a collection. Collections are just for your own organization — they're not visible to anyone else.

---

## Three ways to create a deck

### 1. Generate with AI — right inside the app

Go to [New deck](/decks/new) and pick the **Generate with AI** tab. Paste in your lecture notes, slides, or any study material and the app will turn it into a full deck instantly. This is the fastest option for most people — no copying prompts, no uploading files.

### 2. Use any AI outside the app, then import

If you'd rather use ChatGPT, Claude, Gemini, or any other AI on your own, use the prompt template below. Copy it, replace `[TOPIC]` with your subject, and paste it into any AI. It will output a JSON file in exactly the right format. Then go to [New deck → Import JSON](/decks/new?tab=import) and drop the file in (or paste the JSON directly).

This is useful when you want more control over the prompt, or when you already have a long document open in another AI tool.

<CodeBlock label="Prompt template" code={LLM_PROMPT} />

### 3. Use the API directly

If you're a developer, running scripts, or building tools, you can create decks programmatically via the REST API. Generate an API key in [Settings → API Keys](/settings) and POST directly to `/api/decks`. The rest of this page documents everything the API can do.

---

## Authentication

All API endpoints are authenticated with Bearer API keys. Generate a key in [Settings](/settings) under **API Keys**, then pass it as an `Authorization` header on every request.

```
Authorization: Bearer YOUR_API_KEY
```

Keys are scoped to your account. All write operations (create, update, delete) require a valid key. Some read operations (fetching public decks and course collections) work without authentication.

## Decks

### List your decks

<EndpointCard method="GET" path="/api/decks" auth="required" />

Returns all decks owned by your account, sorted by most recently updated.

<CodeBlock label="Response (200)" code={DECKS_LIST_RESPONSE} />

<ErrorTable errors={[
  { code: "401", message: "Missing or invalid API key" },
]} />

---

### Create a deck

<EndpointCard method="POST" path="/api/decks" auth="required" />

Creates a new deck with cards in a single request. External image URLs in `coverImage` and `back.image` are mirrored to storage automatically.

<FieldTable fields={[
  { field: "name",                    type: "string",     required: true,  note: "Deck title" },
  { field: "description",             type: "string",     required: false, note: "Short description" },
  { field: "visibility",              type: "enum",       required: false, note: "\"PUBLIC\" or \"PRIVATE\" — defaults to \"PRIVATE\"" },
  { field: "coverImage",              type: "string URL", required: false, note: "Deck cover — mirrored to storage on import" },
  { field: "cards[].front.title",     type: "string",     required: true,  note: "Question or prompt shown on the front" },
  { field: "cards[].front.description", type: "string",   required: false, note: "Extra context on the front" },
  { field: "cards[].back.title",      type: "string",     required: true,  note: "Answer shown on the back" },
  { field: "cards[].back.description", type: "string",    required: false, note: "Explanation or additional detail" },
  { field: "cards[].back.image",      type: "string URL", required: false, note: "Card image — mirrored to storage on import" },
]} />

<CodeBlock label="Request body" code={DECK_CREATE_BODY} />
<CodeBlock label="Response (201)" code={DECK_CREATE_RESPONSE} />
<CodeBlock label="curl" code={DECK_CREATE_CURL} />
<CodeBlock label="fetch" code={DECK_CREATE_FETCH} />

<ErrorTable errors={[
  { code: "400", message: "Invalid JSON body" },
  { code: "401", message: "Missing or invalid API key" },
  { code: "422", message: "Missing required field — see error message for details" },
]} />

---

### Get a deck

<EndpointCard method="GET" path="/api/decks/:id" auth="optional" />

Returns a deck and its full card list. Public decks are accessible without authentication. Private decks require the owner's API key.

<CodeBlock label="Response (200)" code={DECK_GET_RESPONSE} />

> **Note on card format:** Cards in this response use `question`/`answer` fields. This differs from the `front`/`back` structure used when *creating* a deck — see the [format note](#cards) in the Cards section.

<ErrorTable errors={[
  { code: "401", message: "Deck is private — provide the owner's API key" },
  { code: "404", message: "Deck not found" },
]} />

---

### Update a deck

<EndpointCard method="PATCH" path="/api/decks/:id" auth="required" />

Update one or more metadata fields on a deck you own. All body fields are optional — send only what you want to change.

<FieldTable fields={[
  { field: "title",       type: "string", required: false, note: "New deck title" },
  { field: "description", type: "string", required: false, note: "New description — send null to clear" },
  { field: "visibility",  type: "enum",   required: false, note: "\"PUBLIC\" or \"PRIVATE\"" },
  { field: "coverImage",  type: "string", required: false, note: "New cover image URL — send null to clear" },
]} />

<CodeBlock label="Request body" code={DECK_PATCH_BODY} />
<CodeBlock label="Response (200)" code={DECK_PATCH_RESPONSE} />

<ErrorTable errors={[
  { code: "401", message: "Missing or invalid API key" },
  { code: "403", message: "Deck belongs to a different user" },
  { code: "404", message: "Deck not found" },
  { code: "422", message: "No valid fields to update" },
]} />

---

### Delete a deck

<EndpointCard method="DELETE" path="/api/decks/:id" auth="required" />

Permanently deletes a deck and all its cards. This cannot be undone.

Returns `204 No Content` on success.

<ErrorTable errors={[
  { code: "401", message: "Missing or invalid API key" },
  { code: "403", message: "Deck belongs to a different user" },
  { code: "404", message: "Deck not found" },
]} />

---

### Export a deck

<EndpointCard method="GET" path="/api/decks/:id/export" auth="optional" />

Downloads a deck as a `.json` file in the same format accepted by **Import JSON**. Useful for backing up a deck, sharing it, or re-importing it elsewhere. Public decks are accessible without authentication; private decks require the owner's API key.

The response is sent as a file download (`Content-Disposition: attachment`) with a filename derived from the deck title.

<CodeBlock label="Response (200)" code={DECK_EXPORT_RESPONSE} />

<ErrorTable errors={[
  { code: "401", message: "Deck is private — provide the owner's API key" },
  { code: "404", message: "Deck not found" },
]} />

---

## Cards

> **Format note:** When *creating a deck* (`POST /api/decks`), cards use a `front`/`back` structure. When reading or writing *individual cards*, they use `question`/`answer` directly.
>
> | Context | Shape |
> |---------|-------|
> | `POST /api/decks` — bulk create | `{ front: { title, description? }, back: { title, description?, image? } }` |
> | `POST /api/decks/:id/cards` — add one | `{ question, answer, imageUrl? }` |
> | `GET /api/decks/:id` — returned cards | `{ id, question, answer, imageUrl, position }` |

### List cards

<EndpointCard method="GET" path="/api/decks/:id/cards" auth="optional" />

Lists all visible cards in a deck. Same auth rules as [Get a deck](#get-a-deck): public decks need no auth, private decks require the owner's key. Hidden cards (flagged by the community) are excluded.

<CodeBlock label="Response (200)" code={CARDS_LIST_RESPONSE} />

<ErrorTable errors={[
  { code: "401", message: "Deck is private — provide the owner's API key" },
  { code: "404", message: "Deck not found" },
]} />

---

### Add a card

<EndpointCard method="POST" path="/api/decks/:id/cards" auth="required" />

Adds a single card to a deck you own. If `position` is omitted the card is appended after the last existing card.

<FieldTable fields={[
  { field: "question", type: "string", required: true,  note: "Front text" },
  { field: "answer",   type: "string", required: true,  note: "Back text" },
  { field: "imageUrl", type: "string", required: false, note: "Card image URL — send null to clear" },
  { field: "position", type: "number", required: false, note: "Zero-based position — defaults to last" },
]} />

<CodeBlock label="Request body" code={CARD_CREATE_BODY} />
<CodeBlock label="Response (201)" code={CARD_CREATE_RESPONSE} />

<ErrorTable errors={[
  { code: "401", message: "Missing or invalid API key" },
  { code: "403", message: "Deck belongs to a different user" },
  { code: "404", message: "Deck not found" },
  { code: "422", message: "Missing required field: question or answer" },
]} />

---

### Update a card

<EndpointCard method="PATCH" path="/api/decks/:id/cards/:cardId" auth="required" />

Updates a card's question, answer, or image. All fields are optional — send only what you want to change.

<FieldTable fields={[
  { field: "question", type: "string", required: false, note: "New front text" },
  { field: "answer",   type: "string", required: false, note: "New back text" },
  { field: "imageUrl", type: "string", required: false, note: "New image URL — send null to remove" },
]} />

<CodeBlock label="Request body" code={CARD_PATCH_BODY} />
<CodeBlock label="Response (200)" code={CARD_PATCH_RESPONSE} />

<ErrorTable errors={[
  { code: "401", message: "Missing or invalid API key" },
  { code: "403", message: "Deck belongs to a different user" },
  { code: "404", message: "Deck or card not found" },
  { code: "422", message: "No valid fields to update" },
]} />

---

### Delete a card

<EndpointCard method="DELETE" path="/api/decks/:id/cards/:cardId" auth="required" />

Permanently deletes a card. Returns `204 No Content` on success.

<ErrorTable errors={[
  { code: "401", message: "Missing or invalid API key" },
  { code: "403", message: "Deck belongs to a different user" },
  { code: "404", message: "Deck or card not found" },
]} />

---

## Collections

Collections are groups of decks. There are two kinds:

- **Personal collections** — created by you, visible only to your account. `courseCode` is `null`.
- **Course collections** — admin-managed groups linked to a Dal course code (e.g. `"INFO 2390"`). Publicly readable by anyone.

A deck can belong to at most one personal collection and any number of course collections simultaneously.

### List collections

<EndpointCard method="GET" path="/api/collections" auth="required" />

Returns your personal collections (excluding course collections).

<CodeBlock label="Response (200)" code={COLLECTIONS_LIST_RESPONSE} />

To list all course collections globally, add `?type=course`:

<EndpointCard method="GET" path="/api/collections?type=course" auth="required" />

<CodeBlock label="Response (200)" code={COLLECTIONS_COURSES_RESPONSE} />

<ErrorTable errors={[
  { code: "401", message: "Missing or invalid API key" },
]} />

---

### Create a collection

<EndpointCard method="POST" path="/api/collections" auth="required" />

Creates a new personal collection. To bootstrap a course collection for a Dal course that doesn't exist yet, include `courseCode` — this creates the collection and adds an empty community deck automatically.

<FieldTable fields={[
  { field: "name",       type: "string", required: true,  note: "Collection name" },
  { field: "courseCode", type: "string", required: false, note: "Dal course code (e.g. \"BIOL 2030\") — creates a community-owned course collection. Normalised to uppercase. One collection per course code globally." },
]} />

**Personal collection**

<CodeBlock label="Request body" code={COLLECTION_CREATE_BODY} />
<CodeBlock label="Response (201)" code={COLLECTION_CREATE_RESPONSE} />

**Course collection**

<CodeBlock label="Request body" code={COURSE_COLLECTION_CREATE_BODY} />
<CodeBlock label="Response (201)" code={COURSE_COLLECTION_CREATE_RESPONSE} />

> Course collections are community-owned — any verified Dal student can create one, but only admins can rename or delete them. You can create at most **3 course collections per 24 hours**.

<ErrorTable errors={[
  { code: "401", message: "Missing or invalid API key" },
  { code: "409", message: "A collection for that course code already exists — see id in response" },
  { code: "422", message: "Missing required field: name" },
  { code: "429", message: "Rate limit exceeded — max 3 course collections per 24 hours" },
]} />

---

### Get a collection

<EndpointCard method="GET" path="/api/collections/:id" auth="optional" />

Returns a collection and its full deck list. Course collections are publicly readable — no auth needed. Personal collections require the owner's API key.

<CodeBlock label="Response (200)" code={COLLECTION_GET_RESPONSE} />

<ErrorTable errors={[
  { code: "403", message: "Personal collection — provide the owner's API key" },
  { code: "404", message: "Collection not found" },
]} />

---

### Rename a collection

<EndpointCard method="PATCH" path="/api/collections/:id" auth="required" />

<FieldTable fields={[
  { field: "name", type: "string", required: true, note: "New collection name" },
]} />

<CodeBlock label="Request body" code={COLLECTION_PATCH_BODY} />
<CodeBlock label="Response (200)" code={COLLECTION_PATCH_RESPONSE} />

<ErrorTable errors={[
  { code: "401", message: "Missing or invalid API key" },
  { code: "403", message: "Collection belongs to a different user, or is a course collection (admin only)" },
  { code: "404", message: "Collection not found" },
  { code: "422", message: "Missing required field: name" },
]} />

---

### Delete a collection

<EndpointCard method="DELETE" path="/api/collections/:id" auth="required" />

Deletes the collection. Decks inside are **not** deleted — they are simply unlinked. Returns `204 No Content` on success. Course collections can only be deleted by admins.

<ErrorTable errors={[
  { code: "401", message: "Missing or invalid API key" },
  { code: "403", message: "Collection belongs to a different user, or is a course collection (admin only)" },
  { code: "404", message: "Collection not found" },
]} />

---

### Add deck to collection

<EndpointCard method="PUT" path="/api/collections/:id/decks/:deckId" auth="required" />

Adds a deck to a collection. Both the collection and the deck must belong to your account. Idempotent — adding a deck already in the collection is a no-op.

If the deck is already in another **personal** collection it is moved automatically. Course collection memberships are never affected.

<CodeBlock label="Response (200)" code={COLLECTION_ADD_DECK_RESPONSE} />

<ErrorTable errors={[
  { code: "401", message: "Missing or invalid API key" },
  { code: "403", message: "Collection or deck belongs to a different user" },
  { code: "404", message: "Collection or deck not found" },
]} />

---

### Remove deck from collection

<EndpointCard method="DELETE" path="/api/collections/:id/decks/:deckId" auth="required" />

Removes a deck from a collection. The deck itself is **not** deleted. Returns `204 No Content` on success.

<ErrorTable errors={[
  { code: "401", message: "Missing or invalid API key" },
  { code: "403", message: "Collection belongs to a different user" },
  { code: "404", message: "Deck is not in this collection" },
]} />

---

## Status codes

All endpoints follow standard HTTP conventions.

<ErrorTable title="Status codes" errors={[
  { code: "200", message: "Success" },
  { code: "201", message: "Created" },
  { code: "204", message: "Success — no body returned" },
  { code: "400", message: "Invalid JSON body" },
  { code: "401", message: "Missing or invalid API key" },
  { code: "403", message: "Forbidden — resource belongs to a different user" },
  { code: "404", message: "Resource not found" },
  { code: "422", message: "Validation error — check the error field in the response body for details" },
  { code: "5xx", message: "Server error — wait a moment and retry" },
]} />
